version: "3.9"

services:
  app:
    image: rag-leaderboard-app:latest
    build:
      context: .
    ports:
      - "7860:7860"
    expose:
      - "7860"
    tty: true
    env_file:
      - .env
    volumes:
      - ./data:/app/data
